@model IEnumerable<lesson06.Models.VdkSong>

@{
    ViewBag.Title = "Vdk: danh sách bài hát";
}

<h2>Pvv: danh sách bài hát</h2>

<p>
    @Html.ActionLink("Create New", "VdkCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VdkTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VdkAuthor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VdkArtist)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.vdkYearRelease)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VdkTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VdkAuthor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VdkArtist)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.vdkYearRelease)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
